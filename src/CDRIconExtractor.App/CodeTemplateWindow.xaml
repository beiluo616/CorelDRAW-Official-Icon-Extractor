<Window x:Class="CDRIconExtractor.App.CodeTemplateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="代码模板"
        Width="610" Height="445"
        MinWidth="520" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Icon="Assets/f10ai.ico"
        Background="#F5F7FA">
    <Window.Resources>
        <Style x:Key="RoundCodeButton" TargetType="Button">
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="13,6"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Background" Value="#EEF2F7"/><Setter TargetName="Chrome" Property="BorderBrush" Value="#94A3B8"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Chrome" Property="Background" Value="#E2E8F0"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryCodeButton" TargetType="Button" BasedOn="{StaticResource RoundCodeButton}">
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText" FontSize="16" FontWeight="SemiBold" Foreground="#172B4D" Margin="2,0,0,9"/>

        <!-- Only the code editor uses a dark theme; the rest of the app/window remains light. -->
        <Border Grid.Row="1" Background="#0B0F14" BorderBrush="#222B36" BorderThickness="1" CornerRadius="7" ClipToBounds="True">
            <TextBox x:Name="CodeTextBox"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12.5"
                     Foreground="#F8FAFC"
                     CaretBrush="White"
                     SelectionBrush="#315A9B"
                     Padding="11"
                     BorderThickness="0"
                     Background="Transparent"/>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,9,0,0">
            <Button Content="复制全部" Click="CopyAll_Click" MinWidth="88" Style="{StaticResource PrimaryCodeButton}"/>
            <Button x:Name="SaveButton" Content="另存为" Click="Save_Click" MinWidth="88" Style="{StaticResource RoundCodeButton}"/>
            <Button Content="关闭" IsCancel="True" MinWidth="76" Style="{StaticResource RoundCodeButton}"/>
        </StackPanel>
    </Grid>
</Window>
